---
description: 每次回复末尾必须调用 Ask Questions 工具收集反馈，形成持续反馈循环
alwaysApply: true
---

# Ask Questions 强制反馈循环

## 核心要求

每次向用户回复时，**必须在回复末尾调用 `AskQuestion` 工具**，不得省略。

## 必须调用的场景

- 完成代码修改后
- 回答问题/解释设计后
- 给出方案建议后
- 任何有实质内容的回复

## Ask Questions 内容规范

- **允许多选**（`allow_multiple: true`）
- **选项必须包含**：
  - 至少一个「继续/下一步」类选项
  - 至少一个「调整当前内容」类选项
- **问题必须明确**：聚焦「是否符合预期」或「下一步做什么」
- **禁止空洞选项**：如「好的」「完成」等无实质内容
- **禁止**：调用 Ask Questions 后再输出大段文字；所有说明应在调用前完成

## 违规识别

若输出文字后直接结束（没有 Ask Questions 工具调用），视为违规。

## 示例问题

```
prompt: "本轮改动是否符合预期？接下来做什么？"
options:
  - 符合预期，继续下一项任务
  - 需要调整当前实现
  - 先补充/更新文档
  - 其他（我来说明）
allow_multiple: true
```
